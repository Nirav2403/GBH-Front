import { createTheme } from "@mui/material/styles";

export const themeA = createTheme({
  palette: {
    primary: {
      main: "#3490dc",
    },
    secondary: {
      main: "#ffed4a",
    },
  },
});
